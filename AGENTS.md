# TRIBUNAL agent instructions

Read `CONTEXT.md`, then `.roles/ROLE.md`, before changing the repository.
Build visible rights-bounded caseflow features before reusable infrastructure.
Use public aggregates or synthetic examples; make no individual justice decision.
Run `cargo test --all-targets` and the README example before committing.
